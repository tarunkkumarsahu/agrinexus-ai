# By default, the Next.js same-origin /api proxy forwards to FastAPI on the laptop.
# Leave this variable unset for laptop and trusted private-Wi-Fi phone review.
# Optional advanced override (direct browser request, not suitable for phone preview):
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
